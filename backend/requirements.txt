# Core
fastapi==0.104.1
python-multipart>=0.0.6
uvicorn[standard]==0.24.0
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.1.0

# LangChain & OpenAI (compatible with langchain-core>=1.2.x for Gemini)
langchain>=1.0.0
langchain-openai>=1.0.0
langchain-google-genai>=2.0.0
openai>=2.20.0,<3.0.0

# LangGraph checkpoint persistence (PostgreSQL backend for workflow state)
# psycopg[binary] includes self-contained libpq — no system libpq required in Docker
langgraph-checkpoint-postgres>=3.0.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# PostgreSQL
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0

# Encryption for secrets
cryptography>=42.0.0

# WebSocket (>=14 required by langgraph-sdk; uvicorn[standard] accepts >=10.4)
websockets>=14,<16

# PDF
PyPDF2>=3.0.0

# Auth
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
resend>=2.0.0
redis[asyncio]>=5.0.0
email-validator>=2.0.0

# Utilities
python-dotenv==1.0.0
dateparser>=1.2.0
httpx==0.25.2
cloudinary>=1.36.0
tenacity==8.2.3

